apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: nodeport
    port: 27017
    protocol: TCP
    targetPort: 22
  selector:
    vm.kubevirt.io/name: {{ .Release.Name }}
  type: NodePort

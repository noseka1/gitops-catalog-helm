apiVersion: logging.openshift.io/v1
kind: ClusterLogging
metadata:
  name: instance
  namespace: {{ .Release.Namespace }}
spec:
  managementState: Managed
  logStore:
    type: lokistack
    lokistack:
      name: loki
  collection:
    type: vector
